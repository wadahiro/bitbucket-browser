import { createAction } from 'redux-actions';

export const fetchBranchInfo = createAction('bitbucket/fetchBranchInfo', () => {

    return;
});

export const actions = {
    fetchBranchInfo
}